# Anthropic API Key (bắt buộc nếu không dùng Claude CLI)
# Lấy API key tại: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ====== PROXY CONFIGURATION (Tùy chọn) ======
# Sử dụng custom API proxy thay vì API chính thức của Anthropic
# ANTHROPIC_BASE_URL=http://localhost:8080
# ANTHROPIC_AUTH_TOKEN=your-proxy-auth-token
#
# Override model aliases (cho proxy mapping)
# ANTHROPIC_MODEL=your-default-model
# ANTHROPIC_DEFAULT_OPUS_MODEL=your-opus-replacement
# ANTHROPIC_DEFAULT_SONNET_MODEL=your-sonnet-replacement
# ANTHROPIC_DEFAULT_HAIKU_MODEL=your-haiku-replacement
# CLAUDE_CODE_SUBAGENT_MODEL=your-subagent-model

# Tùy chọn - API key cho Automaker server
# AUTOMAKER_API_KEY=your-automaker-api-key

# Tùy chọn - CORS origin
# CORS_ORIGIN=http://localhost:3007

# Tùy chọn - Thư mục được phép truy cập (trong container)
# ALLOWED_ROOT_DIRECTORY=/projects

# Tùy chọn - Claude CLI OAuth credentials (cho macOS users)
# CLAUDE_OAUTH_CREDENTIALS=

# Tùy chọn - Cursor CLI OAuth token
# CURSOR_AUTH_TOKEN=